export const enum accessLevel {
  public = 1,
  followersOnly,
  private,
}

export const enum liveSessionStatus {
  ready = 1,
  opened,
  breaked,
  closed,
}

export const enum Role {
  participant = 1,
  organizer,
}
